# 001 · 倪海厦 Skill · 专家知识组织研究 · 研究记录

## 2026-09-06 · 外部导览、整体架构与发布

按用户要求发布本项目。增加首次访问的三个阅读入口、整体架构 SVG 与可打开原图、资料加工流程及非医疗教学示例。架构图区分外部准备、关联整理工具、nihaixia 知识产物、宿主模型与 App 两条路径，明确部分资料复用和未实施的建设建议。项目 README 和总站封面提供相同导引；页面配置规范网址及架构分享预览图。

发布前在独立目录检查待发布版本：桌面原图文字及连线可读，手机 390 × 844 无页面横向溢出，原图链接、页面锚点和图片加载正常；场景选择与医案搜索通过。新增 `architecture.svg` 为原创示意图，`architecture-guide.png` 为其真实浏览器渲染截图；更新首屏截图。未执行上游 Skill、App 或问答模型。

GitHub Pages 已启用 `main` 分支 `/docs` 发布源。本次只提交 001 的文档、页面、图片及对应共享样式和生成索引，不包含其他项目未提交内容。发布前运行目录同步、目录校验、JavaScript 语法和变更格式检查。

## 2026-09-06 · 三个问题的展示重组

新增 [strategy.md](strategy.md)，按“当前方案、参考架构、对我们的价值”组织说明。页面区分本仓库静态展示、上游 Skill 的文件检索与宿主模型、关联 App 的本地数据与程序规则。资料处理链路标明外部文本提取和脚本边界。

参考架构区提供扫描件与表格、自然语言问答、精确统计与筛选、跨文档关系、少量资料精读五种场景。每种场景展示组合步骤、适用原因、与当前方案的关系、实施前的验证事项及一手来源。方案均为研究建议，未接入服务或作性能排名。六类能力详情增加可迁移到本仓库的组织方法。

本次浏览器验证：五种场景切换正确；搜索“医案”显示对应详情和借鉴价值；无结果提示及重置恢复 6 / 6 项；“搜索与读取”流程切换正确。1280 × 720 桌面、390 × 844 手机均未出现页面横向溢出，页面 ID 无重复。手机可切换扫描件场景。检查后恢复浏览器默认尺寸。

完成元数据同步，`npm run sync`、`npm run check`、页面 JavaScript 语法检查及 `git diff --check` 均通过。

更新真实截图：首屏 `cover.png`、医案详情 `capabilities.png`、检索步骤 `workflow.png`、手机参考架构 `mobile.png`；新增自然语言问答参考组合 `architectures.png`。这些图片仅展示本仓库页面，不代表上游模型效果。

## 2026-09-06 · 资料到产品的架构追踪

新增 [architecture.md](architecture.md)。除原技能快照外，追踪关联的 tcm-distiller（a3c105f）和 nihaixia-app（9b8dff9，master 分支），检查整理规程、索引脚本、资料提取、JSON 合并、资源加载、问卷及本地规则调用链。

关键发现：Skill 路径由宿主模型执行文件检索和回答规程；App 路径由资源包、Repository、Dart 规则引擎与 Flutter 界面组成。SQLite 主要存储用户状态。转换脚本含固定本机路径与预先整理的数据，不能视为已验证的一键端到端构建。只读相关源码，未执行上游脚本、安装技能或运行 App；三个版本不代表统一发布组合。

## 2026-09-06 · 能力整理与展示

- **问题**：项目的能力、机制、场景、扩展方向与本仓库的可迁移方法是什么？
- **版本**：`68ce4bf21351a35911d173224c1bc66db3e4e754`（2026-08-19）。
- **环境**：Windows / PowerShell、Node.js 22.15.0、Python 3.10 静态服务、Codex 内置浏览器。
- **创建方式**：按约定使用 `npm run project:new` 分配 `001-nihaixia`；PowerShell 下使用 `npm.cmd --% run project:new -- --slug nihaixia --name "倪海厦 Skill · 专家知识组织研究" --repo https://github.com/jangviktor-web/nihaixia` 保留参数。

### 资料检查

查看固定版本目录、技能入口、医案表、表达规则、蒸馏导航与校验记录。运行自编的 `verify-upstream.mjs`，生成 [verification.json](verification.json)。仅统计目录和文本结构，未保存或执行上游提示词、讲义和安装脚本。

结果为 14 个模块、入口 132,476 字节、1,257 个医案编号行（含 1 个占位行），根目录未见许可证文件。这些统计不证明医学内容正确、病例独立或疗效有效。

### 展示实现

- 六项能力按主题筛选，支持关键词搜索、无结果提示及重置。
- 详情包括实现、场景、问题示例、能力边界和固定版本来源。
- 五步流程展示路由、索引、读取、分析和输出检查。
- 独立呈现结构发现、扩展建议和本仓库可迁移的知识组织方法。
- 问题示例仅作解释，未接入 AI 或诊疗服务。

### 验证记录

浏览器检查结果：

| 检查 | 结果 |
| --- | --- |
| 默认能力列表 | 显示 6 / 6 项 |
| 资料与学习筛选 | 显示 3 / 6 项 |
| 搜索“医案” | 显示 1 / 6 项并定位医案详情 |
| 搜索不存在的能力 | 显示无结果提示，可重置为 6 / 6 |
| 清空搜索 | 键盘全选后删除，恢复 6 / 6 |
| 能力详情切换 | 医案检索标题、场景及来源正确更新 |
| 流程切换 | “搜索与读取”显示对应说明 |
| 桌面布局 | 1280 × 720 视口检查，未横向溢出 |
| 手机布局 | 390 × 844 视口检查，未横向溢出，能力详情可切换 |
| 浏览器日志 | 检查时无 warning / error |
| 首页入口 | 封面与项目卡片显示正常，从首页可进入本地演示 |
| 目录同步与校验 | 已执行 npm run sync / npm run check，通过 |
| 语法与变更格式 | 两个 JavaScript 文件语法检查及 git diff --check 通过 |

截图存放于 `docs/assets/projects/001-nihaixia/`：`cover.png` 为首屏；`capabilities.png` 为医案能力详情；`workflow.png` 为检索步骤；`mobile.png` 为手机能力列表。它们均来自本次展示，不作为上游回答效果证据。

### 未覆盖范围

- 未安装上游技能，未测试模型输出、引用准确性或宿主兼容性。
- 未逐项审核医学内容与资料授权，未统计独立患者数。
- 未实现检索增强、数据清洗、模型评测或后端部署。
- 本次完成本地子项目与入口，未推送远程或声称线上可访问。
