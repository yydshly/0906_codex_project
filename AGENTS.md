# 仓库维护约定

- 默认使用中文撰写研究记录，命令和代码标识保留原文。
- 根 README 只放摘要、项目索引、图片速览和维护入口；详细内容放在对应项目目录。
- 项目目录使用 `三位或更多位数字编号-英文短名`；编号永久保留，展示顺序通过 `projects.json` 的 `order` 调整。
- 使用 `npm run project:new -- ...` 创建项目。元数据修改后执行 `npm run sync` 和 `npm run check`。
- README 标记区间与 `docs/index.html` 由脚本生成；页面样式在 `docs/assets/style.css` 中维护。
- 截图放在 `docs/assets/projects/<编号-短名>/`，说明图片展示内容；不使用虚构截图或研究结论。
- 新项目记录上游仓库、研究版本、运行方式和许可证。避免直接导入整个上游仓库，按实际研究需要保存实验代码。
- `docs/` 是静态站点发布目录。含后端的项目应单独部署，并在登记表填写外部演示地址。
